import { BasicInfoCard } from "./Cards";

export const createCards = ({ checked, classes, disabled, style, name, theme, title, checkImg, value, handlers, checkboxGroup }) => {
    let parent = document.createElement("div");
    parent.classList.add("max-w-50");

    return parent;
};
